import React from "react";

import styles from './TimerElement.module.css';

const TimerElement = () => {

    return (
        <div className={`${styles.timerElementWrapper}`}>
            
        </div>
    )
}

export default TimerElement